// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'VwzksSwT8Z6QkPoVzrq5uAQ',
    authDomain: 'bic-borrow.firebaseapp.com',
    databaseURL: 'https://bic-borrow.firebaseio.com',
    projectId: 'bic-borrow',
    storageBucket: 'bic-borrow.appspot.com',
    messagingSenderId: '1056777379951',
    appId: "1:1056777379951:web:bcde3259fbd2b273e02d9a",
    measurementId: "G-MMNGYF8CQQ"
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
